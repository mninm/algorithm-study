package programmers.level0

//정수가 들어 있는 배열 num_list의 원소의 순서를 거꾸로 뒤집은 배열을 return

class p016_reversed_array {
    fun solution(num_list: IntArray): IntArray {

        return num_list.reversedArray()
    }
}